# 学习笔记

## 一、项目开发过程中的关键知识点

### 1. CWRU 数据集解析

CWRU 数据集的 .mat 文件命名规则较为复杂，需要通过正则表达式解析：

- 正常数据：`normal_{负载}_{编号}.mat`
- 故障数据：`12k_Drive_End_{类型}{直径}{位置}_{负载}_{编号}.mat`
  - 类型：IR（内圈）、B（滚动体）、OR（外圈）
  - 直径：007/014/021（对应 0.007/0.014/0.021 英寸）
  - 位置：外圈故障才有 @6/@3/@12（对应时钟方位）

.mat 文件内部变量名格式为 `X{编号}_DE_time`，需要动态匹配。

### 2. 振动信号预处理

- **带通滤波**：使用 Butterworth 滤波器，`filtfilt` 零相位滤波避免相位偏移
- **去趋势**：`scipy.signal.detrend` 消除传感器零漂
- **滑动窗口**：1024 点窗口、512 点步长，通过 numpy 索引技巧高效切片
- **z-score 标准化**：按每个窗口独立标准化，消除幅值差异

### 3. 特征工程（20维）

**时域特征（12个）**：均值、绝对均值、标准差、方差、RMS、峰峰值、峰值、峭度、偏度、波形因子、峰值因子、脉冲因子、裕度因子

**频域特征（8个）**：频谱均值、频谱标准差、频谱质心、频谱RMS、主频幅值、主频位置、频谱峰度、频谱偏度

关键发现：**峭度（kurtosis）** 对轴承故障冲击最敏感，正常信号峭度约为 3，故障信号显著升高。

### 4. 机器学习模型对比

| 模型 | 随机划分准确率 | 5折交叉验证 | 按文件分组 |
|------|--------------|------------|-----------|
| KNN | 高 | 中等 | 低 |
| SVM | 高 | 中等 | 中等 |
| 随机森林 | 0.997 | 0.891 | 0.633 |

### 5. 数据泄漏问题（重要发现）

这是本项目最重要的学习收获之一：

- **随机划分**：同一文件的不同窗口被分到训练集和测试集，导致数据泄漏，准确率虚高（0.997）
- **按文件分组**：整个文件作为一组，训练集和测试集无重叠，更贴近真实场景（0.633）
- **教训**：时间序列或信号数据评估时，必须按来源分组，不能随机打乱

### 6. Flask Web 开发

- 前后端分离：前端通过 REST API 获取 JSON 数据
- 多窗口投票：诊断时对所有窗口取概率均值，提升稳定性
- 数据抽稀：原始信号最长 24 万点，抽稀至 3000 点便于前端展示
- SQLite 持久化：诊断记录、告警记录、知识库四张表

### 7. ECharts 可视化

- 波形图、频谱图、健康趋势图三图联动
- ECharts 库本地化（`web/static/js/echarts.min.js`），避免 CDN 不可达
- 趋势图三线叠加：历史数据、预测线、预警阈值

## 二、遇到的问题与解决方案

### 问题1：Windows 中文路径下 joblib 报错

**现象**：`RandomForestClassifier(n_jobs=-1)` 训练时报 `UnicodeEncodeError`

**原因**：Windows 用户名为中文（张昊），joblib 多进程临时目录路径含中文，序列化失败

**解决**：在代码开头设置 `JOBLIB_TEMP_FOLDER` 环境变量为 `C:\Windows\Temp` 等 ASCII 路径

### 问题2：Flask 服务启动后网址打不开

**现象**：浏览器访问 127.0.0.1:5000 显示无法连接

**原因**：Python 环境缺少 numpy、scipy、scikit-learn、flask 等依赖

**解决**：`pip install numpy pandas scipy scikit-learn matplotlib flask flask-cors joblib`

### 问题3：模型准确率虚高

**现象**：随机划分测试集准确率 99.7%，但实际泛化能力差

**原因**：同一 .mat 文件的滑动窗口被分到训练集和测试集，数据泄漏

**解决**：采用 GroupKFold 按文件分组评估，得到真实准确率 63.3%

### 问题4：前端大数据量渲染卡顿

**现象**：原始信号 24 万点直接传给前端，页面卡顿

**解决**：后端等间隔抽稀至 3000 点，频谱抽稀至 1500 点

## 三、常用命令记录

```bash
# 安装依赖
pip install -r requirements.txt

# 启动 Web 系统
python bearing_system.py

# 训练模型
python scripts/train_classifier.py

# 构建特征矩阵
python scripts/build_features.py

# API 测试
python scripts/test_api.py
```

## 四、参考资料

1. CWRU 轴承故障数据集：https://engineering.case.edu/bearingdatacenter
2. scikit-learn 官方文档：https://scikit-learn.org/
3. Flask 官方文档：https://flask.palletsprojects.com/
4. ECharts 官方文档：https://echarts.apache.org/
