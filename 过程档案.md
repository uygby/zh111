# 过程档案

## 一、vibe coding 学习与实践

### 1.1 提示词工程实践

**任务拆解策略**：将"轴承故障诊断系统"拆解为六个阶段逐步实现：
1. 阶段一：环境配置与数据获取
2. 阶段二：数据预处理与特征工程
3. 阶段三：故障分类模型训练与评估
4. 阶段四：健康预测与知识推理
5. 阶段五：Web系统前后端开发
6. 阶段六：系统联调测试与文档整理

**约束说明模板**：每次向AI提出开发需求时，明确说明：
- 技术栈：Python + Flask + scikit-learn + ECharts + SQLite
- 数据来源：CWRU公开数据集
- 输出格式：函数签名、返回值类型、异常处理
- 验收标准：可运行、有测试、有注释

**关键prompt示例**：
```
请实现一个CWRU数据加载模块，要求：
1. 解析文件名获取故障类型、负载、编号等元信息
2. 读取.mat文件中DE通道的振动信号
3. 支持正常数据和故障数据两种命名格式
4. 每个函数写清楚参数和返回值
5. 包含自测代码
```

### 1.2 上下文工程实践

- **项目规则文件**：在项目中维护README.md，说明项目结构、运行方式、数据来源
- **让AI先读代码再改**：修改已有模块时，先让AI读取相关文件，理解现有结构后再提出修改
- **保留会话上下文**：多轮对话中保持项目背景信息，避免重复说明

### 1.3 规格驱动开发

按照"需求文档 → AI制定计划 → 分步实现 → 逐步验收"的流程：
1. 先编写需求规格说明书，明确功能点
2. 让AI根据需求制定开发计划
3. 按模块分步实现，每完成一个模块立即测试验收
4. 发现问题及时回滚或修正

## 二、AI出错案例及纠正过程

### 案例1：Windows中文路径下joblib报错

**现象**：训练随机森林模型时，`n_jobs=-1` 多进程并行报 `UnicodeEncodeError`

**AI初始建议**：修改系统编码设置或切换到Linux环境

**纠正过程**：
- 实际原因：Windows用户名为中文（张昊），joblib多进程临时目录路径含中文，序列化失败
- 正确解决：在代码开头设置 `JOBLIB_TEMP_FOLDER` 环境变量为 `C:\Windows\Temp` 等ASCII路径
- 最终在 `classifier.py` 和 `bearing_system.py` 中都加入了此修复

### 案例2：模型准确率虚高问题

**现象**：随机划分测试集准确率99.7%，但实际泛化能力差

**AI初始分析**：认为模型效果很好，建议增加更多特征

**纠正过程**：
- 人工审查发现：同一.mat文件的滑动窗口被随机分到训练集和测试集，造成数据泄漏
- 正确做法：采用GroupKFold按文件分组评估，得到真实准确率63.3%
- 这个发现成为项目的核心亮点，在报告和PPT中重点说明

### 案例3：Flask服务后台启动失败

**现象**：后台运行Flask服务时，端口无法访问

**AI初始建议**：检查端口占用、防火墙设置

**纠正过程**：
- 实际原因：Flask的debug=True模式会启动reloader子进程，在后台运行时冲突
- 正确解决：将 `debug=True` 改为 `debug=False`
- 修改后服务稳定运行

### 案例4：前端大数据量渲染卡顿

**现象**：原始信号24万点直接传给前端，页面卡顿

**AI初始建议**：使用Web Worker或Canvas优化渲染

**纠正过程**：
- 更简单有效的方案：后端等间隔抽稀至3000点，频谱抽稀至1500点
- 既保证了视觉效果，又大幅提升了加载速度
- 在 `_decimate()` 函数中实现

## 三、git提交记录

| 序号 | 提交说明 | 主要内容 |
|---|---|---|
| 1 | Initial commit | 项目初始化，README，.gitignore |
| 2 | 阶段一：数据下载与环境配置 | 下载脚本，依赖清单 |
| 3 | 阶段二：数据预处理与特征工程 | preprocessing.py, feature_extraction.py |
| 4 | 阶段三：分类模型训练与评估 | classifier.py, 训练脚本, 模型文件 |
| 5 | 阶段四：健康预测与知识推理 | health_prediction.py, knowledge_base.py |
| 6 | 阶段五：Web系统开发 | Flask后端, 前端页面, SQLite数据库 |
| 7 | 阶段五：系统测试 | API测试脚本, 测试报告 |
| 8 | 阶段六：文档整理 | 系统设计报告, 使用说明, 答辩准备 |
| 9 | 修复Windows中文路径joblib问题 | classifier.py兼容性修复 |
| 10 | 新增单文件整合版 | bearing_system.py, 启动脚本 |
| 11 | 新增选题说明、学习笔记、报告 | 文档补充 |

## 四、AI工具使用记录

### 4.1 使用工具
- 豆包AI编程助手：代码生成、调试、文档撰写
- VS Code：代码编辑与运行

### 4.2 使用策略
1. **先规划后编码**：先让AI输出模块设计和函数签名，确认后再实现
2. **小步提交**：每个功能模块完成后立即测试，通过后再继续
3. **人工审查**：AI生成的代码逐行阅读，理解逻辑后再采纳
4. **错误驱动**：遇到报错先让AI分析原因，再结合实际情况判断
5. **保留回滚能力**：使用git管理，重大修改前先提交，方便回滚

### 4.3 人工接管场景
- 数据泄漏问题的发现与处理（AI未能主动识别）
- Windows环境兼容性问题的最终定位
- 报告内容的审核与修改（确保自己理解所有内容）
- 答辩PPT的结构设计与内容取舍

## 五、学习心得

1. AI是高效的编码助手，但不能替代人的判断和审查
2. 提示词的质量直接决定输出质量，任务拆解越细效果越好
3. 遇到AI反复出错时，应缩小问题范围，提供最小复现案例
4. 数据泄漏等隐蔽问题需要人工审查才能发现
5. 版本控制是vibe coding的安全网，小步提交很重要
